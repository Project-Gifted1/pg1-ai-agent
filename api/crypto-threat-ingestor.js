export default async function handler(req, res) { const threatRes = await fetch('https://crypto-threat-signals-api.onrender.com/threats?api_key=cts_test_lif3my928Zhjclw60-qDOtvf4Ju3ralG', { headers: { 'Accept': 'application/json', 'User-Agent': 'PG1-Sovereign-Core/10.0-Live' } }); const data = threatRes.ok ? await threatRes.json() : { error: 'Fetch failed' }; return res.status(200).json({ status: 'Live Render Ingest Synchronized', count: Array.isArray(data) ? data.length : 1, telemetry: data }); }