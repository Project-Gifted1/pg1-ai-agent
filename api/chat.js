export const config = { api: { bodyParser: { sizeLimit: '10mb' }, maxDuration: 60 } };
export default async function handler(req, res) { return new Response(JSON.stringify({ status: 'ACTIVE', smoketest: process.env.SMOKETEST_API_KEY ? 'LINKED' : 'PENDING' }), { status: 200, headers: { 'Content-Type': 'application/json' } }); }