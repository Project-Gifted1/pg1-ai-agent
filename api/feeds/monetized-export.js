export default async function handler(req, res) { const { format, license_key } = req.query; if (!license_key) return res.status(401).json({ error: 'Unauthorized' }); try { const g = await fetch('https://api.gumroad.com/v2/licenses/verify', { method: 'POST', headers: { 'Content-Type': 'application/x-www-form-urlencoded' }, body: new URLSearchParams({ product_id: process.env.GUMROAD_PRODUCT_ID, license_key }) }); const d = await g.json(); if (!d.success || d.purchase.refunded) return res.status(403).json({ error: 'Forbidden' }); } catch (e) { return res.status(500).json({ error: 'Internal Error' }); } return res.status(200).json({ type: 'bundle', status: 'active' }); }