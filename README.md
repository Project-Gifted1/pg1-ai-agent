# pg1-ai-agent
Private local multimodal AI client⁠.

## Automated deployment

Deploy Vercel + Cloudflare Worker + Supabase in one command:

```bash
npm run deploy:auto
```

Validate wiring without deploying:

```bash
npm run deploy:auto:dry-run
```
